{
  "name": "automivex",
  "private": true,
  "version": "1.0.0",
  "description": "Automivex monorepo with separately deployable frontend and backend apps",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build --workspace frontend && npm run check --workspace backend",
    "build:frontend": "npm run build --workspace frontend",
    "check": "npm run check --workspace backend",
    "check:backend": "npm run check --workspace backend",
    "test": "npm run test --workspace backend",
    "test:backend": "npm run test --workspace backend",
    "validate": "npm run build && npm run test",
    "preview:frontend": "npm run preview --workspace frontend",
    "start:backend": "npm run start --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
