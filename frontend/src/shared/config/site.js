// This file contains build-time configuration only.
// All content that needs to be editable is now served from /api/site-content.
// If you need to add new content, add it to backend/content/public-site-content.json,
// update shared/schemas.js, and refactor the component to fetch from useSiteContentQuery().

// Animation and UI constants (not content)
// Add build-time constants here as needed

export const noopExport = null;

