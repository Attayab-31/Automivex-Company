/**
 * Frontend Application Constants
 * Centralized constants for routes, timing, and other magic values
 */

export const ROUTES = {
  ADMIN_LOGIN: "/admin/login",
  ADMIN_DASHBOARD: "/admin/dashboard",
};

export const UI = {
  /// Duration (ms) for which status messages are displayed before auto-clearing
  MESSAGE_DISPLAY_DURATION_MS: 3000,
};
