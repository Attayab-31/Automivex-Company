// Prebuild script for Vercel
// Previously validated backend URL, but now vercel.json has hardcoded rewrites
// This script is kept for future use but no longer performs validation

process.exit(0);
